# backend/requirements.txt  ←  THIS IS THE FIX
Flask==2.3.3
flask-cors==4.0.0
google-generativeai==0.3.2
requests==2.31.0
beautifulsoup4==4.12.3
gunicorn==21.2.0
python-dotenv==1.0.0

# lxml pre-compiled wheel (no C compiler needed!)
lxml==5.3.0 --only-binary=all

# OR (alternative) use lxml-all-in-one that bundles everything
# lxml-all==0.6.0
