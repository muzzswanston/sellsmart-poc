# frontend/nixpacks.toml
[phases.setup]
  nixPkgs = ["...", "flutter"]   # ← this installs Flutter automatically

[phases.build]
  cmds = [
    "flutter pub get",
    "flutter build web --release"
  ]

[start]
  cmd = "echo 'Static site - no start command needed'"
